import {useSession} from "next-auth/react";

export default function HeroText() {

    return (
        <div>
            <h1 className="text-white font-pixel2 absolute z-50 inset-0 flex items-center font-light justify-center tracking-widest text-4xl select-none">The
            world of Minecraft</h1>
        </div>

)

}